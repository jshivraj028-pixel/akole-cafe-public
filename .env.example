# MongoDB Atlas Connection String
# Replace <username>, <password>, and <cluster-url> with your MongoDB Atlas database credentials
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/akole_cafe?retryWrites=true&w=majority

# Server Port
PORT=5000

# JWT Authentication Secret
JWT_SECRET=akole_cafe_secret_key_2026
